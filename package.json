{
  "name": "firestore-bigquery-changelog",
  "version": "0.2.6",
  "description": "SDK to log Firestore document changelog directly to BigQuery",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.0.0"
  },
  "peerDependencies": {
    "firebase-functions": ">=4.0.0"
  },
  "devDependencies": {
    "firebase-functions": "^4.4.1",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}